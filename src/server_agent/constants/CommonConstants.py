import pathlib

DATASET_PATH = str(pathlib.Path(__file__).parent.parent.parent / "server_new" / "data" / "files")

SANDBOX_PATH = str(pathlib.Path(__file__).parent.parent.parent / "data" / "sandbox")
